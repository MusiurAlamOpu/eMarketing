import React, { useEffect, useState } from "react";

const Contact = () => {
  const array = [1, 3, 5, 2, 5];
  const [number, setNumber] = useState(null);
  // array.map((num) => setNumber(num));
  // setNumber(array);

  console.log(number);
  return <div style={{ textAlign: "center" }}>coming soon....</div>;
};

export default Contact;
